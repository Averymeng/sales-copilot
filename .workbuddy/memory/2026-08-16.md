# 2026-08-16

## 觅客精灵 preview 改动（6 项）
- 确认盯盘能力：当前无专门「盯盘 Agent」；异常规则化呈现（首页异常提醒/档案健康度预警=紧急，待办=非紧急）。建议在文档明确归属。
- 删除侧边栏「新客开拓」发光点（span.dot），所有页面。
- 客户档案 / 客户投放 默认态重构为「筛选窗口(客户+阶段) + 氛围感留白」，选中客户后内联渲染对应模板（无列表、去重我的客户）。
- 新增 qa.html「精灵问答」独立页（突出 AI 问答），侧边栏入口在「知识库」下方。
- 首页实时热点/行业动态由 3 条扩至 7 条；早报由 3 指标扩为 AI摘要+4指标+3关键变化。
- app.js 新增 renderClientFilter（[data-client-filter]），styles.css 新增 .filterbar/.fb-select/.ambient/.cf-detail。
- 文档 觅客精灵-页面设计规范.docx 追加 v0.7（去重决策）与 v0.8（本轮修订）。
- 未 push（用户未说 git）。

## 第二轮改动（8 项，晚间）

### 1. 盯盘 Agent 澄清
- 用户纠正：待办/日程的紧急/非紧急分类 + AI 问答「今天有什么紧急」= 这就是盯盘能力。不是「没有盯盘」，而是分散在现有系统里（待办 urgency + 首页异常 + AI chat）。
- 结论：**当前产品已具备盯盘功能**，只是未统一品牌为「盯盘 Agent」。建议后续在 PRD 中明确归拢。

### 2. 侧边栏 精灵问答 稳定性修复
- **根因**：knowledge.html 缺少 `<a href="qa.html">精灵问答</a>` 条目。
- 已修复：所有 10 个页面（含 knowledge.html）现在均有完整一致的侧边栏条目。

### 3. 行业动态尺寸
- 用户重申规则：「只改我提到的部分」。确认 行业动态 与 行业大盘 均为 `1fr 1fr`（等宽），无需 revert。

### 4. 精灵问答页重设计（参考 ReviveAI 图）
- 改为居中欢迎态：大图标 + "Hey，我是觅客精灵"（渐变文字）+ 描述 + 大搜索栏(圆角pill+发送按钮) + 建议 chips
- 首次交互后切换到聊天面板模式（welcome 隐藏，chat panel 显示）
- 更干净、更聚焦对话体验

### 5. 首页大重构（参考 Revival Analytics dashboard 图）
- **删除**：早报底部的 3 条独立「关键变化 .li」（三异常变化）
- **新增**：业绩卡片增加 SVG donut 饼图（赛道占比分布+图例），移至右上与早报并列
- **早报可视化**：KPI 指标下增加 mini-bar-chart（7 日趋势柱状）
- **合并事项**：新增「关键变化 & 异常事项」卡片（合并原 异常提醒+关键变化概念），含 2 项异常 + 2 项动态
- **左右排列**：今日3件事（左）| 关键变化&异常事项（右）
- 新布局：[早报|业绩饼图] → [3件事|异常事项] → [大盘表|行业动态]

### 6. 待办页修复
- 右侧新增「AI 本周洞察」卡片（填充周报下方空白区域）
- 周报生成内容加 `max-height:400px;overflow-y:auto` 防止撑破布局

### 7. 客户投放页重设计（参考 Analytics dashboard 图）
- 一键生成按钮移至筛选栏右侧（top-right 并列）
- KPI 行扩充至 5 个指标，每个带 7 日趋势柱状图
- 版位对比表增加「占比」列 + mini donut（信息流 65%/搜索 35%）
- 投放素材改为 rich mat-card（封面大图+标题+描述+3 项数据 stats+复用按钮）
- 新增「近 4 周消耗趋势」大柱状图
- 智能诊断从 2 条扩至 3 条（新增素材疲劳预警）
- 账户概览增加「月预算」「目标 CPA」

### 8. 客户档案模板全面改造（客户手册风格）
- **新增 profile-header 组件**：大头像+客户名+标签云(meta spans)+一句话描述
- **新增「关键人」section**：联系人卡片（头像+姓名+角色+决策影响力+沟通风格+偏好）
- **新增「销售笔记 & 客户画像」**：核心！沟通偏好、客户性格、合作节奏、历史痛点、当前风险、增投机会
- **投放概况精简**：从原来的大卡片压缩为 4 个 mini-stats 数字 + 「查看详细投放→」按钮
- **删除「快捷动作」**（编辑档案/添加到待办）— 不重要，空间让给销售笔记
- **时间线升级为 tl-rich**：增加日期列(.date)、更多条目、更详细的描述
- 所有 7 个客户模板均已按此结构重写（预合作 3 个 + 已有投放 4 个）

## 第三轮改动（图标丑 + 业绩重设计）
- 用户：今日早报 & 我的业绩「太丑、图标不好看」。要求：先删除今日早报已有图标，再新设计其他的。
- **今日早报**：删除标题 emoji 图标 📋（仅删图标，未改结构/数据）。
- **我的业绩**：重设计（移除 📊 emoji 标题）。原 4 段式 donut 改为单一渐变 ring（目标完成率 68%，stroke 9 + round caps）；核心指标改为 2×2 网格（本月消耗/在投账户/管理客户/平均留资成本）；赛道构成改为平滑分段 pill 条 + 图例；底部新增「本月新增线索 9（较上月+2）」。
- dashboard.html 内联 style 中 `.donut*` 替换为 `.perf*/.ring*/.compo*/.perf-foot*`。
- 未 push（用户未说 git）。

## 第四轮：v1.0 终稿交付（用户说"全部 git" + 要最全文档+数据库+CSV联动）
- 早报 mini-bar 裸柱状图被用户截图吐槽"这啥玩意" → 删除 `.mini-bar/.tk/.spark`，改为干净 `.brief` 卡片排版。已 push（0e4b0f6）。
- v0.9 文档同步：PRD 与页面规范均追加第 4 Agent（盯盘/值守 Agent）设计 + 修正 v0.8"无盯盘 Agent"描述 + 10 页 IA + AI 介入点。已 push。
- **v1.0 终稿全套交付并 push（1f5371d→origin/main）**：
  - `觅客精灵-PRD-v1.0.docx`：整合 v0.2→v0.9 为连贯终稿，含 4 Agent 架构(§7)、值守 Agent 触发频率/紧急阈值(§8.2)、10 页 IA、完整 17 表 Schema(附录A)、AI 介入点表(附录B)、变更史(附录C)。
  - `觅客精灵-页面设计规范-v1.0.docx`：设计原则/Token/10 页布局线框/交互规范(Drawer/浮层/响应式)。
  - 数据库：`db/schema.sql`(17 表 DDL+索引) + `db/seed/*.csv`(17 种子, 15 客户) + `db/README.md`。
  - CSV 联动 HTML：`preview/data.html` —— 由构建脚本读取 `db/seed/*.csv` 生成可排序数据浏览器（侧边栏切表、点表头排序）。**注意**：此为本机内联版；用户提到的「资料库」在线 CSV↔HTML 因网络/在线空间依赖未走，已用等价本地方案替代，已向用户说明可改走在线 资料库。
- 用户重申的硬规则：① 只改提及的部分；② git = push 到 Averymeng/sales-copilot。

## 第五轮：盯盘 Agent 概念澄清 + 在线资料库（用户："不联网也算agent？用没用llm？" + "资料库要正式上线能用的"）
- **概念澄清（重要）**：用户质疑「不联网的也算 agent？」「有没有用到 llm？」
  - 结论：① Agent 由「LLM + 工具 + 触发」定义，不取决于是否联网；② 值守 Agent **不调联网搜索工具(Tavily)**，但**调用 LLM(DeepSeek)推理/合成**，因此仍是 Agent；③ 架构分两层：异常检测层=规则引擎(定时指标比对/SQL，无LLM，仅判定触发)；值守/汇总层=LLM(读检测结果+待办+其他Agent产物，判紧急度/生成自然语言早报/答"今天有什么紧急")。
  - PRD v1.0 §7.4 已改写为明确说明（原"不联网搜索"一句已扩充为两层设计说明）。
- **在线资料库（正式上线可用，非本地）**：用 资料库 skill 将 `db/seed/*.csv` 全部导入 workbuddy.cn 空间（17 张表全部成功，node_block_id 见 `db/ONLINE_DATABASES.md`）。
- **联动网页（CSV↔HTML）**：新建 `preview/accounts-data.html`（绑定 accounts 表 On6f0fYhTft4GSwsfEJIgN，db.query 渲染 + db.addRecord 新增 + localStorage 表单缓存），通过 lint_database_sdk_usage（MINDX_DBSDK_LINT_OK），已 publish → https://www.workbuddy.cn/space/d/FbwluMkPLEw4neFuidpW79
- 关键命令：import_csv.py（CSV→Database）、get_database_schema.py、lint_database_sdk_usage.py、import_html.py；token 经 connect_open_platform(skill_id=library) 获取，脚本用 `--token-stdin` 读 stdin 首行。
- 映射文件：`db/ONLINE_DATABASES.md`（17 表 ID+链接 + 联动页链接）。
- 未 push（用户本轮回未说 git）。本地还有改动：PRD v1.0 小改 + 新增 accounts-data.html + ONLINE_DATABASES.md + data.html。
- 注意：token 有效期 1800s，批量导入需在有效期内完成。

## 数据审查修正（21:56 轮）
- 用户审查数据发现：客户数量不足/缺兴趣教育；campaigns.objective 含品牌曝光；质疑 agent_runs/chat_messages/events/proposals 为何"规定好"；问 comm_style「直接」/creatives.cover_style/tasks.source 含义。
- 修复（本地 CSV + 在线资料库 re-import 同 ID 覆盖）：
  - accounts 15→29：新增「兴趣教育」赛道 6 家（美术宝/编程猫/VIP陪练/弈秋围棋/动因体育/凯叔讲故事）+ 各赛道 +2（师图/考德上/达内/北大青鸟/启德/金吉列/奥鹏/弘成）。
  - contacts +14、leads +4（兴趣教育）、campaigns 改 objective 全为 线索收集/留资转化（删品牌曝光/品牌+留资/品牌种草）+3 条、creatives +3。
  - schema.sql 赛道注释补「兴趣教育」。
- 澄清（非改动）：agent_runs/chat_messages/events/proposals 的 seed 行均为「示例样本」，运行时由 LLM/skill 真实生成并写入；我们只预定义表结构，不预定义内容。comm_style「直接」=直来直去沟通风格（样本里混了渠道维度微信/电话，建模不一致，待用户确认是否拆分）。
- 未 push（用户未说 git）。

## 字段修正 v1.1（22:16 轮）
- 用户困惑：打开 preview HTML 看不到数据变化 → 澄清 preview 是静态 UI mockup，不绑库；真实数据在 db/seed/*.csv（线下17）+ 资料库在线表（线上17，同ID）。
- campaigns.objective 收紧为 进线/开口/留资 三选一（删品牌类）；name=投放计划名称须与 objective 对齐。
- contacts 拆 comm_style → comm_channel(微信/电话/线下) + comm_style(直接/温和/数据导向)；creatives.cover_style → type(视频/图文)，封面由用户供图非Canva；tasks.source「AI生成」→「AI监测」。
- 本地 CSV + schema.sql + 在线4表(campaigns/contacts/creatives/tasks) re-import 覆盖 OK；PRD 附录A 加 v1.1 字段修订+种子vs运行时澄清。
- **22:20 用户"继续" → 已 commit+push（3c18516→origin/main）**：含 schema/CSV/PRD v1.1 全部修正 + accounts-data.html + ONLINE_DATABASES.md。

## 待用户决策（未执行）
- 是否将 preview 静态页也改为「读线上库、显示29家客户」真正联动？目前 preview 仍是 UI mockup，真实数据在 db/seed/*.csv（线下17）+ 资料库在线表（线上17）。
- **22:30 用户"继续" → 已新建 `preview/workbench.html`（数据联动工作台总览）并发布在线**：聚合读取 7 张核心表(accounts/campaigns/leads/tasks/contacts/creatives/agent_runs)，KPI 实时统计 + 客户/在投计划/待办/线索列表；URL https://www.workbuddy.cn/space/d/I9e07pICJ3BV3N5frgTjJH；已 commit+push（39a38b0）。ONLINE_DATABASES.md 已补该页链接。

## 第六轮：真·应用脚手架（22:39 起）
- 用户确认两决策：**运行库=Neon Postgres**、**现在就搭脚手架**。
- 已搭 **Next.js 15 App Router + TS + Tailwind** 真实应用（此前只有静态 preview + 在线资料库）：
  - 配置：package.json / tsconfig / next.config / tailwind / postcss / .env.example
  - `lib/db.ts`：DB 层「有 DATABASE_URL 连 Neon，否则回退读 db/seed/*.csv」，今日即可本地跑通
  - `lib/llm.ts`(DeepSeek) / `lib/search.ts`(Tavily) / `lib/csv.ts`
  - `agents/`：orchestrator(总控路由) + discovery(商机发现) + monitor(竞品/行业监测) + watcher(盯盘/值守·规则层detectAnomalies+LLM合成) 全部骨架，接口清晰
  - `app/`：layout + Sidebar(10页IA) + 首页(工作台总览) + accounts/campaigns/leads/tasks(读真实数据) + qa(client) + knowledge/industry/proposals/reports(stub) + api/overview + api/qa
  - `scripts/seed.ts`：DATABASE_URL 设好后 `npm run db:seed` 灌 Neon
- 验证：`next build` 通过（15 路由）；`next start` 起服务，/ 渲染29客户+KPI(HTTP200)，/api/overview 返回 kpis 全量，/api/qa「今天有什么紧急」→ 路由到盯盘/值守 Agent 且 DeepSeek 真出回复（.env.local 已含 DEEPSEEK_API_KEY，LLM 实跑非占位）。
- **待办**：next@15.1.6 有 CVE-2025-66478 安全告警，部署前应升到 patched 版本（待用户确认是否现在升级）。
- 已 commit 本地（23c0a5b）；**push 被环境 502 CONNECT tunnel 拦截**，GitHub 推送暂未成功，待网络恢复或用户手动 push。

## 第七轮：并行收尾（23:17 起，用户点头）
- **push 重试成功**：`78dfba5..eb7e61d` 已上 origin/main（之前 502 是环境瞬时网络，恢复后一次过）。
- 用户批准并行做三件：①升级 Next 修 CVE ②加深首页/精灵问答 ③写 Neon 傻瓜文档。
- **①升级 Next 修 CVE**：next 15.1.6 → **15.5.23**（backport 最新 15.x，含 CVE-2025-66478 修复）；npm install + next build 通过。
- **②加深首页**：新增「我的业绩」饼图(conic-gradient，按投放目标分布) + 「今日3件事」(按紧急度排序取3) + 「关键变化」(数据衍生) + 「行业动态」；app/page.tsx 重写。
- **②加深精灵问答**：app/qa/page.tsx 改为聊天式气泡 UI，显示路由到的 Agent 标签(🛡盯盘/值守/🔍商机发现/📡竞品行业监测/🧠总控编排) + 建议问题 chips + 加载态；api/qa 返回 agent 字段已对上。
- **③Neon 傻瓜文档**：`NEON_SETUP.md`（建项目→拿连接串→填 .env.local→npm run db:seed→验证徽标）。
- 验证：build 15 路由通过；/ 渲染4个新区块(HTTP200)；/api/qa「今天有什么紧急」→agent=盯盘/值守 + DeepSeek 真回复；/qa HTTP200。
- 已 commit+push（b1c2253→origin/main）。

## 第八轮：答疑 + 文档（23:27 起）
- 用户疑问①：担心"一直出现需要删除东西然后确认"。澄清：**从未删除任何项目源文件**；出现的"确认"是两类——(a)我用 AskUserQuestion 提的方向选择题（Neon vs 资料库等），不是删除；(b)Bash 命令的权限确认弹窗。唯一"reset"是早前撤销一个误含 .next/node_modules 的本地提交（git reset --soft，文件未动、已干净重提）。后续只在真正不可逆动作（git push 共享库/删文件/部署）才确认。
- 用户要求②：**NEON_SETUP.md 给 Word 版** → 用 python-docx 生成 `觅客精灵-Neon接入指南.docx`（已生成）。
- 用户要求③：**评测/badcase 等页面全部深化完再做**（顺序确认：与 PRD 第2阶段一致）。
- 用户要求④：**重新核对"构思→完成全流程(带进度)"与 PRD 要求的步骤差异**。已抽 PRD"实现路线"：
  - 第1阶段(今天)：搭 Next.js 脚手架 + 跑通 P0 主链路 + 可演示原型 + **部署 Vercel**
  - 第2阶段(后续)：补齐 P1(精灵问答/知识库/盯盘Agent) + **跑评测/收集 badcase/迭代 prompt**
  - 第3阶段(再后续)：P2 增强 + 升级向量库/数据源(企查查/Canva)
  - MVP：P0=早报/新客开拓(线索+调研报告)/我的客户/客户档案/客户投放/提案与复盘/待办日程；P1=精灵问答主对话页/知识库/智能对话浮窗/盯盘Agent；本期不做=微信接入/录音转写/续约流程/真实小红书API/Canva真生图
  - 差异要点：①部署Vercel 我滞后(列第9阶段未做) ②智能对话浮窗 PRD P1 要求、我未做 ③P0主链路(早报独立页/客户档案手册化/调研报告)部分未实现 ④Neon真库我提前做了(PRD第1阶段默认模拟数据) ⑤安全升级CVE是我额外加(PRD无) ⑥Canva真生图需求已变更(用户供图)应划掉 ⑦评测badcase顺序一致(页面深化后)
- 待：把差异对比呈现给用户，并 commit+push 本次(Word文档)。

## 第九轮：页面深化（用户定序：先深化再部署，23:32 起）
- 用户方向：**先把页面深化完，再部署 Vercel**（回答了上一轮差异①②的优先级问题）。
- 用户反馈：`觅客精灵-Neon接入指南.docx` 在聊天里"看不到" → 已用 python-docx 校验文件有效(48段)，并用 present_files 打开为可下载 artifact；说明 docx 不内联渲染。
- 已建页面深化任务清单(40-43)：①智能对话浮窗 ②客户档案手册化 ③客户投放详情 ④剩余stub页(新客/调研/行业/知识库/提案复盘)。
- **40 智能对话浮窗 ✅**：新增 `components/ChatWidget.tsx`（右下角🧚气泡，展开聊天，调 /api/qa，带 Agent 标签+建议问题），挂到 `app/layout.tsx` 全局；PRD P1 要求的"智能对话浮窗"补上。
- **41 客户档案手册化 ✅**：`app/accounts/page.tsx` 重写。每客户卡片含：基本信息(track/product/city/stage/spend/budget/health色标) + 关键人(contacts按account_id，决策力标签) + 投放计划(campaigns标签) + 跟进时间线(followups按account_id + events按related_account)。`lib/db.ts` 新增 getFollowups()/getEvents()。
- 验证：build 通过(15路由)；/accounts HTTP200 渲染4区块；/ 含浮窗🧚。
- 待 commit+push；继续 42/43。
